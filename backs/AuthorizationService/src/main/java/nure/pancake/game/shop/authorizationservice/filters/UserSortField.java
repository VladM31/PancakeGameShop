package nure.pancake.game.shop.authorizationservice.filters;

public enum UserSortField {
    ID,
    ROLE,
    EMAIL,
    ACTIVE,
    NICKNAME,
    LAST_NAME,
    FIRST_NAME,
    BIRTH_DATE,
    USER_STATE,
    PHONE_NUMBER,
    SELECTED_CURRENCY,
    DATE_REGISTRATION
}
